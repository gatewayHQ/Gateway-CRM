# ─────────────────────────────────────────────────────────────────────────────
# Gateway CRM — Environment Variables
# Copy this file to .env.local for local dev, or add to Vercel → Settings →
# Environment Variables for production.
# ─────────────────────────────────────────────────────────────────────────────

# ── Supabase (required) ──────────────────────────────────────────────────────
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Service role key — used by server-side API routes only (never expose to browser)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key   # Supabase → Settings → API → service_role

# ── Email Sending via Resend (add to Vercel to enable sequences + server-side send) ──
# Get your API key at resend.com → API Keys
# RESEND_FROM must be a domain you've verified in Resend
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
RESEND_FROM="Gateway Real Estate <hello@yourdomain.com>"

# ── Cron Security ────────────────────────────────────────────────────────────
# Protects /api/sequence-run from being triggered by anyone other than Vercel cron.
# Generate with: openssl rand -hex 32
GATEWAY_CRON_SECRET=your-random-secret-here

# ── Microsoft Graph / Outlook (optional — per-agent "Connect Outlook") ───────
# Azure Portal → App registrations → "Gateway CRM" (or create one).
# Platform: Web (NOT Single-page application) — the OAuth code exchange runs
# server-side in api/email-send.js, so the client secret never reaches the
# browser. Redirect URI must be added under that Web platform, exact match:
#   https://your-app.vercel.app/api/email-send?action=outlook-callback
# API permissions (delegated, admin consent not required): User.Read, Mail.Send,
# Mail.ReadWrite, Mail.ReadBasic, offline_access, Calendars.Read,
# Calendars.ReadWrite, Contacts.Read.
MICROSOFT_CLIENT_ID=00000000-0000-0000-0000-000000000000
MICROSOFT_CLIENT_SECRET=your-client-secret-value   # Certificates & secrets → New client secret
# Directory (tenant) ID from the app's Overview page. Use the brokerage's own
# tenant to restrict sign-in to their M365 org; 'common' also works (any
# work/school or personal Microsoft account) if that restriction isn't wanted.
MICROSOFT_TENANT_ID=your-tenant-id
# Must exactly match a Redirect URI registered on the app (see above). Falls
# back to https://<request host>/api/email-send?action=outlook-callback if unset
# — fine for local dev, but set this explicitly in Vercel for production so a
# preview-deployment host never gets exchanged for a real code.
MICROSOFT_REDIRECT_URI=https://your-app.vercel.app/api/email-send?action=outlook-callback
# Space-separated Graph scopes requested at connect time. Defaults to the full
# permission set above (incl. Calendars/Contacts) so future sync features don't
# need every agent to reconnect. Override only to narrow it.
# MICROSOFT_GRAPH_SCOPES=openid profile email offline_access https://graph.microsoft.com/User.Read https://graph.microsoft.com/Mail.Send https://graph.microsoft.com/Mail.ReadWrite https://graph.microsoft.com/Mail.ReadBasic https://graph.microsoft.com/Calendars.Read https://graph.microsoft.com/Calendars.ReadWrite https://graph.microsoft.com/Contacts.Read

# AES-256-GCM key used to encrypt Microsoft access/refresh tokens at rest in
# ms_graph_connections. Generate with: openssl rand -base64 32
# REQUIRED once any agent connects Outlook — losing/rotating this key without a
# re-connect flow orphans every stored token (they become undecryptable).
MS_TOKEN_ENCRYPTION_KEY=

# ── Twilio SMS (optional — configure in app Settings → Integrations) ─────────
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-auth-token

# ── BoldSign e-signature (optional) ──────────────────────────────────────────
# Get an API key at app.boldsign.com → Settings → API → API Keys.
# Sandbox vs Live is decided by WHICH key you generate — a Sandbox key never
# sends real email or consumes credits, so use one while developing. There is no
# separate test-mode flag, and the two are SEPARATE ACCOUNTS: template ids,
# sender identities and webhook secrets do not carry over from one to the other.
# Rate limits differ too — Sandbox 50 requests/hour, Live 2,000/hour, per account.
BOLDSIGN_API_KEY=your-boldsign-api-key

# Webhook signing secret — BoldSign → Settings → API → Webhooks → your endpoint →
# "Reveal". REQUIRED: /api/boldsign refuses to process events (503) without it,
# because an unverified endpoint lets anyone who knows the URL mark a real
# signature request completed, declined or expired on a real deal.
BOLDSIGN_WEBHOOK_SECRET=

# Local dev only — process webhook events with NO secret configured.
# Never set this on a deployment that talks to a Live BoldSign account.
# BOLDSIGN_WEBHOOK_INSECURE=true

# Go-live safety valve: verify the signature, LOG the verdict, and process the
# event anyway. Turn this on for the first hours after switching to the Live key
# so a wrong/rolled secret shows up in the logs instead of silently 401-ing every
# document update, then turn it off.
# BOLDSIGN_WEBHOOK_AUDIT_ONLY=true

# Region override. Defaults to the US endpoint; EU-hosted accounts set:
#   BOLDSIGN_API_BASE=https://api-eu.boldsign.com/v1
# BOLDSIGN_API_BASE=https://api.boldsign.com/v1

# ── AI / Claude (optional — agents can also set their own key in Settings) ───
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx

# ── Buffer social posting (optional) ────────────────────────────────────────
BUFFER_ACCESS_TOKEN=your-buffer-token

# ── Branded QR / short-link domain (optional) ────────────────────────────────
# By default, mailing QR codes point at this app's own URL (e.g. your-app.vercel.app).
# To look more professional and hide the CRM's real URL, register a short domain
# (e.g. gatewayre.link or gre.homes), point it at this Vercel project, then set:
#   VITE_PUBLIC_LINK_DOMAIN=https://gatewayre.link
# All QR codes then become https://gatewayre.link/m/<token>. The /m/:token rewrite
# already handles the scan-tracking redirect, so no extra config is needed.
# VITE_PUBLIC_LINK_DOMAIN=https://gatewayre.link

# ── CORS override (recommended in production) ────────────────────────────────
# Defaults to * (all origins), which lets any page on the internet use a
# signed-in agent's own token against /api/boldsign from a tab they didn't open.
# Set to your production origin (comma-separate several) to restrict:
# ALLOWED_ORIGIN=https://your-app.vercel.app,https://crm.yourdomain.com

# ── Logging — Logtrail (optional, https://logtrail.net) ──────────────────────
# 1. Sign up at app.logtrail.net (free plan available, no credit card required)
# 2. Go to Settings → API Keys → create a key with write (wo) or rw scope
# 3. Use a lt_production_... key for Vercel production, lt_development_... for local dev
# 4. Add the key here — logs will start appearing in Logtrail immediately
# LOGTRAIL_API_KEY=lt_production_xxxxxxxxxxxxxxxxxxxx
